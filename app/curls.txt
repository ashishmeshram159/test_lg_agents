
# Testing search agent + tool:
curl -X POST "http://localhost:8000/chat" \
  -H "Content-Type: application/json" \
  -d '{"session_id": "general-1", "message": "Tell me what Kyoto is famous for in autumn."}'


# Testing Math agent + tool:
curl -X POST "http://localhost:8000/chat" \
  -H "Content-Type: application/json" \
  -d '{"session_id": "sw-2", "message": "What is (2024^2 - 2000^2) / 12?"}'



# Testing db agent + tool:
curl -X POST "http://localhost:8000/chat" \
  -H "Content-Type: application/json" \
  -d '{"session_id": "swe-1", "message": "Can you list the last 5 orders with id and amount."}'


# Testing medical agent + tool:
curl -X POST "http://localhost:8000/chat" \
  -H "Content-Type: application/json" \
  -d '{
    "session_id": "test-medical-1",
    "message": "What did my doctor say last time about my diabetes and blood pressure?",
    "customer": {
      "id": "CUST_001",
      "name": "Rahul Sharma",
      "dob": "1992-03-15",
      "email": "rahul@example.com"
    }
  }'
